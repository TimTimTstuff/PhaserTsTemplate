import { expect } from "chai"

describe('Initialize Project', () => {
    it('Tests loaded successfully', () => {
        const testVar = 'myValue'
        expect(testVar).to.equal('myValue');
    })
})